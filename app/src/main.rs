//! `billz-app` — the thin Tauri shell. Phase-0 placeholder.
//!
//! Empty by design: Phase 0 is headless (`core` only). The Tauri shell and `#[tauri::command]`s that
//! delegate into `billz-core` arrive in Phase 1 — see `PLAN.md` §9.

// TODO(phase1): stand up the Tauri shell + connection-manager UI (billz-cwt epic).
fn main() {}
